# TAMA38 Production Environment Variables
# Copy this file to .env.production and update the values as needed

# Database Configuration
POSTGRES_PASSWORD=postgres
# Change the above password in production!

# Backend Configuration
SECRET_KEY=CHANGE-THIS-TO-A-RANDOM-SECRET-KEY-IN-PRODUCTION
JWT_SECRET_KEY=CHANGE-THIS-TO-A-RANDOM-JWT-SECRET-KEY-IN-PRODUCTION
LOG_LEVEL=INFO

# Frontend Configuration
VITE_API_URL=http://localhost:8000
# Change this to your production domain, e.g., https://tama38.yourdomain.com

# CORS Configuration
CORS_ORIGINS=http://localhost:3000
# For production, use your actual frontend URL, e.g., https://tama38.yourdomain.com

# Security Notes:
# 1. Generate strong random keys for SECRET_KEY and JWT_SECRET_KEY
# 2. Use a strong database password
# 3. Update CORS_ORIGINS to match your production frontend URL
# 4. Set LOG_LEVEL to INFO or WARNING in production (not DEBUG)

# To generate secure keys, you can use:
# Python: python -c "import secrets; print(secrets.token_urlsafe(32))"
# PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))

